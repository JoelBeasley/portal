Reset your password

Hi <%= @resource.respond_to?(:full_name) ? @resource.full_name : @resource.email %>,

We received a request to reset your password.
Use this link:
<%= edit_password_url(@resource, reset_password_token: @token) %>
